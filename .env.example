DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash

# Optional pricing overrides (GBP per million tokens). Defaults live in sim/lib/llm.ts.
# VERIFY pricing before launch (build-spec §7).
DS_RATE_IN_MISS=
DS_RATE_IN_HIT=
DS_RATE_OUT=

# Supabase (project: WidgetCo, eu-central-2). The service role key is used by
# the sim's sync step and the web API routes ONLY — it must never reach the
# browser or be committed. In production it lives in GitHub Actions secrets
# and Vercel env vars.
SUPABASE_URL=https://ywfevamxqwqoktgrjjvp.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
