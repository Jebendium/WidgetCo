DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash

# Optional pricing overrides (GBP per million tokens). Defaults live in sim/lib/llm.ts.
# VERIFY pricing before launch (build-spec §7).
DS_RATE_IN_MISS=
DS_RATE_IN_HIT=
DS_RATE_OUT=
